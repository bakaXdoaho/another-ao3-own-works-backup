# ---- 凭据：绝对不能进版本库 ----
# 这里面是你的 Cookie 请求头，等于你的账号。
secrets/

# ---- data/ 默认全部忽略，再逐项放行 ----
# 这是「默认拒绝」名单：**新建的 data/ 子目录一律不进版本库，除非在下面放行。**
# 之所以这样写而不是逐项排除：漏掉一项的后果不对称 ——
# 漏放行只是少备份了点东西，漏排除可能把不该公开的东西提交上去。
#
# ⚠️ 注意 .gitignore **不支持行尾注释**，注释必须自成一行。
#    写成 `!data/ao3/  # 说明` 会被当成一个字面模式，等于没放行。
data/*

# 官方下载件与作品列表原始页 —— 这才是存档本身，值得进版本库：
# git 能告诉你「哪一篇的正文什么时候变过」，而 AO3 本身不提供这个信息。
!data/ao3/

# 评论存档。读者可以自己删评论，删了就只有你的备份还有 —— 不可再生。
!data/comments/

# 派生物，可由 rebuild_index.py 从上面那些文件重建，不必进版本库
data/index.sqlite
data/index.sqlite-*

# ---- 运行记录 ----
# 每次运行的完整输出。想留着就把下面这行删掉；
# 但注意里面有你的作品名、字数、以及给你留言的读者的名字。
code/session_printouts/

# ---- 系统与工具 ----
.DS_Store
.idea/
.vscode/
__pycache__/
*.pyc
